rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: This rule is insecure and for development only.
    // It makes your entire database public.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
